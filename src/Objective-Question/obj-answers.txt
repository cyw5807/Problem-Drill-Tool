#CHOICE
    public class TestRoom {
        TestDoor t = new TestDoor();
        TestRoom() {}
        public static void main(String[] args) {
            TestRoom tt = new TestRoom();
        }
    }
    class TestDoor {
        TestRoom t = new TestRoom();
        TestDoor() {}
        public static void main(String[] args) {
            TestDoor tt = new TestDoor();
        }
    }
which one is correct?
#OPTIONS
A. It does not compile because there are two main()s in one .java file
B. It does not compile because they refer to each other
C. It compiles and runs smothly
D. It compiles and raises exception at running for stack overflow
#USER_ANSWER
It does not compile because there are two main()s in one .java file
#CORRECT_ANSWER
It compiles and raises exception at running for stack overflow
#END

#JUDGE
Multiple inheritances from classes is not allowed in Java.
#OPTIONS
1. T
2. F
#USER_ANSWER
F
#CORRECT_ANSWER
T
#END

#CHOICE
About access control in Java, which statement below is correct?
#OPTIONS
A. A member without any access modifier is default to private
B. A member without any access modifier is default to public
C. A member without any access modifier is default to protected
D. Classes in the same package can access members without any access modifier.
#USER_ANSWER
A member without any access modifier is default to protected
#CORRECT_ANSWER
Classes in the same package can access members without any access modifier.
#END

#CHOICE
    List<String> ls = new ArrayList<String>();
    List<Object> lo = ls;
    lo.add(new Object());
    String s = ls.get(0);
Which statement below is correct?
#OPTIONS
A. It does not compile
B. It compiles but exception raises at line 2
C. It compiles but exception raises at line 3
D. It compiles but exception raises at line 4
#USER_ANSWER

#CORRECT_ANSWER
It does not compile
#END

#JUDGE
制造（new）数组时，要指定数组长度，以便为数组分配内存。
#OPTIONS
1. T
2. F
#USER_ANSWER
F
#CORRECT_ANSWER
T
#END

#CHOICE
About Java containers, which statement below is NOT correct?
#OPTIONS
A. ArrayList holds the elements in a particular sequence.
B. TreeSet cannot have any duplicate elements.
C. LinkedList holds the elements in a particular sequence.
D. List, Set and Map, they all have stream() interface.
#USER_ANSWER

#CORRECT_ANSWER
List, Set and Map, they all have stream() interface.
#END

#JUDGE
枚举类型定义了具有删除功能的方法。
#OPTIONS
1. T
2. F
#USER_ANSWER
T
#CORRECT_ANSWER
F
#END

#JUDGE
read() in InputStream tries to read the next byte of data from the input stream, so it's returns type is byte.
#OPTIONS
1. T
2. F
#USER_ANSWER
T
#CORRECT_ANSWER
F
#END

#JUDGE
Arrays in Java can change its size as needed.
#OPTIONS
1. T
2. F
#USER_ANSWER
T
#CORRECT_ANSWER
F
#END

#JUDGE
We can use an interface as a data type for a variable.
#OPTIONS
1. T
2. F
#USER_ANSWER
F
#CORRECT_ANSWER
T
#END

#CHOICE
    interface I {
        void f();
    }
    class C {
        void f() {};
    }
Which code below compiles?
#OPTIONS
A. class A extends I { void f(); }
B. interface A extends I { void f(); }
C. class A extends C implements I {}
D. interface A implements I { void f(); }
#USER_ANSWER
class A extends C implements I {}
#CORRECT_ANSWER
interface A extends I { void f(); }
#END

#CHOICE
What must be done when throwing an integer as an exception?
#OPTIONS
A. Integers cannot be thrown.
B. Declare integers as throwable.
C. Import the exception class.
D. Encapsulate the integer handler
#USER_ANSWER
Encapsulate the integer handler
#CORRECT_ANSWER
Integers cannot be thrown.
#END

#JUDGE
There is a length() method for all the arrays to represent the number of elements
#OPTIONS
1. T
2. F
#USER_ANSWER
T
#CORRECT_ANSWER
F
#END

#CHOICE
    class DataServer extends Server {
        public String serverName;
        public DataServer() {
            serverName = "Customer Service";
            super(serverName);
        }
    }
Which statement is right?
#OPTIONS
A. Code can pass the compilation
B. Code can pass the compilation but a fault would happen in creating object of DataServer
C. Code can NOT pass the compilation for the error in the 2nd line
D. Code can NOT pass the compilation for the error in the 5th line
#USER_ANSWER
Code can pass the compilation
#CORRECT_ANSWER
Code can NOT pass the compilation for the error in the 5th line
#END

#JUDGE
Objects created in JAVA do not need to be deleted or freed by the programmer.
#OPTIONS
1. T
2. F
#USER_ANSWER
F
#CORRECT_ANSWER
T
#END

